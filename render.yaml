services:
  - type: web
    name: content-resourcer-worker
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build:worker
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
